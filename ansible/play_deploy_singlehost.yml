---
- name: Deploy single-host NSM through frankenstack
  hosts: singlehost

  vars_files:
    - vars/singlehost.yml

  roles:
    - bootstrap_docker_networks
    - deploy_elastic
    - deploy_nsm_suricata 
    - deploy_nsm_moloch
    - deploy_evebox
